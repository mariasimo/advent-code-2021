import { logResults, readInputAsArray } from "../utils";
<br />
const input: number[] = readInputAsArray({ dayNumber:2}); const outputPart1 = 0;
const outputPart2 = 0; logResults(1, [outputPart1, outputPart2]); export {
outputPart1, outputPart2 };