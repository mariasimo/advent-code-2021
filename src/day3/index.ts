import { logResults, readInputAsArray } from "../utils"; const input: number[] =
readInputAsArray({ dayNumber:2}); const outputPart1 = 0; const outputPart2 = 0;
logResults(3, [outputPart1, outputPart2]); export { outputPart1,
outputPart2 };